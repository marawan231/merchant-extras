import 'package:flutter/material.dart';

class TamweelDealsView extends StatelessWidget {
  const TamweelDealsView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}